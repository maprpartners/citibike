kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: citivolclaim
  namespace: mapr-ts
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5G
  storageClassName: influxdb-maprfs
